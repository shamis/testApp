name: Build iOS app

on:
  push:
    branches:
      - 'master'

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install @angular/cli
      run: npm i -D -E @angular/cli
    - name: Use coturiv/setup-ionic
      uses: coturiv/setup-ionic@v1
      with:
        cordova-version: 9
    - name: Xcode version
      run: /usr/bin/xcodebuild -version
    - name: Build
      run: |
          ionic cordova platform add ios
          ionic cordova build ios --prod
          cd platforms/ios
    - name: List
      run: ls -l ./
    - name: Analyze
      run: xcodebuild -project MyApp.xcodeproj -scheme MyApp -sdk iphonesimulator10.3 clean analyze
    - name: Build ios app
      run: xcodebuild -scheme MyApp -workspace MyApp.xcworkspace/ build
    - name: Upload app
      uses: actions/upload-artifact@v1
      with:
        name: app
        path: ./
    
